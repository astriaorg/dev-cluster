apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rollup-chart.fullname" . }}-test-connection"
  labels:
    {{- include "rollup-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rollup-chart.fullname" . }}:80']
  restartPolicy: Never
